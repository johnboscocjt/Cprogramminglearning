#include <stdio.h>

int main() {
    // Declare and initialize a variable
    int var = 5;

    // Use the var++ increment operator
    var++;

    // Display the updated value of the variable
    printf("Updated value of var: %d\n", var);

    return 0;
}

